<%- model_class = Mobile -%>

<div class="panel panel-default monospace">
    <div class="panel-heading"><h3>
        <%= link_to @area.name, area_path(@area) %> / <%= link_to 'Mobiles', area_mobiles_path(@area) %> / <%= @mobile.keywords %>
    </h3></div>
    <div class="panel-body">

        <%= render 'form' %>
    
    </div>
    
    <div class="panel-footer">
        <%= link_to 'Show', area_mobile_path(@area, @mobile) %> |
        <%= link_to 'Back', area_mobiles_path(@area) %>
    </div>
    
</div>
