<%- model_class = Shop -%>

<div class="panel panel-default monospace">
    <div class="panel-heading"><h3>
        <%= link_to @area.name, area_path(@area) %> /
        <%= link_to 'Mobiles', area_mobiles_path(@area) %> /
        <%= link_to "Mobile: #{@mobile.vnum}", area_mobile_path(@area, @mobile) %> /
        New Mobile Shop
    </div>
    <div class="panel-body">    
    
        <%= render :partial => 'form' %>

    </div>

    <div class="panel-footer"><%= link_to 'Back', area_mobile_path(@area, @mobile) %></div>

</div>
