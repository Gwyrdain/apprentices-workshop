D<%= exit.direction %>
<% if exit.description.length > 0 %><%= word_wrap(exit.description, line_width: ApplicationController::WORDWRAP_LENGTH) %>
<% end %>~
<%= exit.keywords %>~
<%= exit.exittype %> <%= exit.keyvnum %> <%= exit.formal_vnum %><% if (exit.name.length > 0) %>
O
<%= exit.name %>~<% end %>