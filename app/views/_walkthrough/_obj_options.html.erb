<% (reset.reset_type == 'E' || reset.reset_type == 'G') ? parent_type = 'sub_reset' : parent_type = 'reset' %> <font color="#505050">[ <a href="#" onclick="toggleStudy(<%= reset.id %>)"><span class="glyphicon gi-sm glyphicon-search"></span></a><% if obj.is_container %> | <a href="#" onclick="toggleContents(<%= reset.id %>)"><span class="glyphicon gi-sm glyphicon-triangle-bottom"></span></a> | <%= link_to 'put', new_area_reset_path(@area, reset, :reset_type => 'P', :parent_type => parent_type, :parent_id => reset.id ) %> | <%= link_to 'insert', new_area_reset_path(@area, reset, :reset_type => 'I', :parent_type => parent_type, :parent_id => reset.id ) %><% end %> ]</font>