 <font color=#181818>[<%= 
link_to "<font color=#181818>Reset with limit: #{reset.val_3}</font>".html_safe, edit_area_reset_path(@area, reset) %> <%=
link_to '<font color=red>x</font>'.html_safe, area_reset_path(@area, reset),
 :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm",
 :default => 'Are you sure?')) } %>]</font>