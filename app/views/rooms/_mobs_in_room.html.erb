<% if @area.resets.where(reset_type: ['M', 'Q']).where(val_4: @room.id).count > 0 %><% @area.resets.order(:val_2).where(reset_type: ['M', 'Q']).where(val_4: @room.id).each do |reset| %><font color='darkmagenta'><%= mobile_info(reset.mob_id, 'ldesc') %></font>
<% end %><% end %>