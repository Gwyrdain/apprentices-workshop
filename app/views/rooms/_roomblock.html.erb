#<%= room.formal_vnum %>
<%= room.name %>~
<%= word_wrap(room.description, line_width: ApplicationController::WORDWRAP_LENGTH) %>
~
<%= @area.area_number %> <%= room.room_flags %> <%= room.terrain %> 
<% room.exits.order(:direction).each do |exit| %>
<%= render 'exits/exitblock', :exit => exit %>
<% end %>
<% room.rxdescs.each do |rxdesc| %>
<%= render 'rxdescs/rxdescblock', :rxdesc => rxdesc %>
<% end %>
S