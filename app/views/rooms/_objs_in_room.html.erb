<% if @area.resets.where(reset_type: 'O').where(val_4: @room.id).count > 0 %><% @area.resets.order(:val_2).where(reset_type: 'O').where(val_4: @room.id).each do |reset| %><% if Obj.exists?(:id => reset.obj_id) %><%=
link_to obj_room_view(reset.obj_id), edit_area_obj_path(@area, reset.obj_id), :target => "_blank" %><%= render 'reset_in_room', :reset => reset %>
<% end %><% end %><% end %>